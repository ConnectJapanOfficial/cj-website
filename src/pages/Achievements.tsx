import { usePageTitle } from "../utilities/hooks";

const Achievements = () => {

    usePageTitle("Achievements");

    return (
        <div>
            
        </div>
    );
};

export default Achievements;