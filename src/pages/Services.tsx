import { usePageTitle } from "../utilities/hooks";

const Services = () => {

    usePageTitle("Services & Fees");
    return (
        <div>
            
        </div>
    );
};

export default Services;